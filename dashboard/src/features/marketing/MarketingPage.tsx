// The /gioi-thieu landing page. Redesigned as a self-contained "quant terminal"
// experience under ./landing (GSAP + Three.js, dark/light, signup-gated funnel).
// The previous section components remain in ./components for reuse but are no
// longer composed here.
export { default } from "./landing/LandingPage"
