# Temporary vhost for ACME validation; the application is not exposed yet.
server {
    listen 103.199.18.160:80;
    server_name trading.iqx.vn;
    location ^~ /.well-known/acme-challenge/ {
        root /srv/iqx-v2/acme;
        try_files $uri =404;
    }
    location / { return 503; }
}
